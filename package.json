{
  "name": "commiq-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm -r dev"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
