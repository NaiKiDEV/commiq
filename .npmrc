; Provenance is intentionally NOT pinned here.
; CI enables it per-publish via NPM_CONFIG_PROVENANCE in .github/workflows/release.yml,
; which keeps a local `pnpm publish` (where provenance cannot be generated) working.
